;;; marginalia.el --- Minibuffer annotations (Marginalia) -*- lexical-binding: t; -*-

;;; Commentary:
;;
;; Marginalia adds helpful annotations to minibuffer completion candidates.
;;

;;; Code:

(use-package marginalia
  :config
  (marginalia-mode 1))

(provide 'completions-marginalia)
;;; marginalia.el ends here
